# Disable optional dependencies that might cause build issues
optional=false
# Use npm cache
cache=/tmp/.npm
# Disable audit for faster builds
audit=false
# Use prebuilt binaries when available
build_from_source=false
